# SessionSifu update channel

`latest.json` and its referenced Debian package are generated by
`packaging/build-deb.sh`. The application accepts packages only from this
repository and verifies both the declared byte size and SHA-256 digest before
performing a user-local GNOME integration update. Windows, macOS and portable
Linux bundles are published as GitHub Release artifacts by
`.github/workflows/release.yml`.
